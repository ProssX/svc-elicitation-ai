# Test environment configuration
# This file overrides .env settings for local testing

# Auth service URL for local testing (outside Docker)
AUTH_SERVICE_URL=http://localhost:8001

# Database URL for local testing
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5434/elicitation_ai
