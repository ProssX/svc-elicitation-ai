# Docker test environment configuration
# This file is used when running tests inside Docker containers

# Auth service URL for Docker testing (use container name)
AUTH_SERVICE_URL=http://svc-users-app:8001

# Database URL for Docker testing (use container name)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@svc-elicitation-db:5432/elicitation_ai
